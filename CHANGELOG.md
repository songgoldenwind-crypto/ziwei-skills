# Changelog

本文件记录面向使用者的功能、规则、兼容性和许可变化。

## 1.1.0 — 2026-09-14

### 解读质量

- 新增排盘、原局概览、专题、限运、规则查询五种任务路由；“算一下”默认做有覆盖面的原局概览，不再缩成财富或身份等级。
- 输出改为按任务组织，取消所有问题强制套用同一五段模板；原局问题不再自动插入当前与下一大限。
- 强化证据链：重要结论至少跨星曜组合、宫位、会照／四化／限运中的两个层面，并把盘面事实与术数推断分开。
- 放宽字段门槛：缺非关键字段时缩小判断范围，不再一律停止整盘。
- 增加现代语境与表达约束，避免单星定案、空宫断无、空泛层级和古代性别角色直译。

### 排盘与分发

- 所有安装目录、`.skill`、Claude ZIP 和插件包均内置排盘源码与数据，不再只有 WorkBuddy 套装自带排盘。
- 真太阳时结果增加校正分钟、校正前后时辰、跨时辰标记、距最近时辰边界分钟及临界标记。
- 运限输出增加原局语义层，以及大限、流年等各层命宫叠原局宫位、四化落宫和流曜落宫。
- 运限日期与时间支持常见中文及斜杠格式；`--no-ai-context` 对本命盘和运限盘一致生效。
- 修正 OpenAI 市场清单引用不存在的 `main` 分支，改为仓库实际使用的 `master`。

## 1.0.0 — 2026-09-14

### 首发

- 新增 `ziwei` Skill：按核盘、格局、正曜组合、四化、大限流年的固定顺序断盘。
- 体系边界：以象义与组合为主，不把五行生克或无限宫干飞化当作主断法；男命女命分断；空宫不得推出六亲不存在。
- 输出收束：默认一个主判断、一条组合链和一个最关键限制；财运、学历、婚姻、限运用相对层级，不输出金额、概率或精确时点。
- 新增 `ziwei-paipan-code`：真太阳时校正、出生四柱、本命盘与运限盘 JSON 输出。排盘只提供结构，断盘仍执行 Skill 规则。
- 安装器、打包器与 WorkBuddy / Claude / OpenAI 插件元数据与 `liuyao-skills` 同一分发布局。
